# Walking skeleton — standalone Compose (phone)

After the spec is approved:

- Single-activity Compose app, Material 3
- Navigation for compact width
- Domain tests without a device
- No ATAK dependency unless the spec later adds a TAK export Adapter

Do not copy this folder until TRACK is `compose-phone`.
