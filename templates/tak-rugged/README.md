# Walking skeleton — TAK plugin (rugged)

After the spec is approved:

- Gradle plugin module that loads in ATAK-CIV
- Large-touch targets, high-contrast, glove-friendly
- Assume intermittent radio / no Play Services
- Compose hosted through an ATAK Adapter
- Domain tests for CoT without the device
- Offline-first is the default unless the spec says otherwise

Do not copy this folder until TRACK is `tak-rugged`.
