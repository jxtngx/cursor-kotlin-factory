# Walking skeleton — TAK plugin (phone)

After the spec is approved:

- Gradle plugin module that loads in ATAK-CIV
- One map overlay or tool button the spec named
- Compose pane hosted through an ATAK Adapter (do not rewrite the map in Compose)
- Domain tests for CoT parse/emit without the device
- Compact-width layout

Do not copy this folder until TRACK is `tak-phone`.
