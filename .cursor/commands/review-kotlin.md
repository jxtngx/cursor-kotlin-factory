# Review Kotlin

Run as `@reviewer`.

Check the current diff against the approved spec and TRACK.

Block on `!!`, `GlobalScope`, secrets, vendored ATAK-CIV, missing tests, or a second product.

Cite Kotlin / Compose / ATAK-CIV docs, not blogs.
