# Walking skeleton — standalone Compose (tablet)

After the spec is approved:

- Single-activity Compose app, Material 3
- List/detail or supporting-pane layout
- Domain tests without a device

Do not copy this folder until TRACK is `compose-tablet`.
